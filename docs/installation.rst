Installation
============

You can install ``nepse-client`` using pip:

.. code-block:: bash

   pip install nepse-client

For the latest development version:

.. code-block:: bash

   pip install git+https://github.com/4mritgiri/NepseClient.git
